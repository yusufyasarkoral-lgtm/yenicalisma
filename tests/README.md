# Tests

All automated tests belong under this directory. Keep API and integration tests
in `tests/api/`, and component or browser tests in `tests/ui/`. Test fixtures
must use synthetic data only; customer names, documents, and production exports
must never be committed here.
